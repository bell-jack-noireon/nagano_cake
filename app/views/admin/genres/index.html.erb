<div class="container">
  <div class="row">
    <div class="col">
      <h4>ジャンル一覧・追加</h4>
      <div>
        <%= form_with model: @genre, url: admin_genres_path do |f| %>
          <div>ジャンル名</div>
          <div><%= f.text_field :name, placeholder: "ジャンル名" %></div>
          <%= f.submit "新規登録" %>
        <% end %>
      </div>
    </div>
  </div>
  <div>
    
  </div>
</div>